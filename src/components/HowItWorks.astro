---
const steps = [
  { n: '01', title: 'Elige tu sabor', text: 'Explora más de 20 sabores y escoge tu favorito (o varios).' },
  { n: '02', title: 'Lo enviamos', text: 'Preparamos tu pedido y llega en 24/48h con embalaje discreto.' },
  { n: '03', title: 'Saca y disfruta', text: 'Sin recargas ni botones. Abre, da una calada y listo.' },
];
---

<section id="como-funciona" class="mx-auto max-w-6xl px-6 py-24 md:py-32">
  <div class="mb-16 max-w-2xl">
    <p class="mb-4 font-display text-sm font-semibold uppercase tracking-[0.3em] text-berry" data-reveal>/ Cómo funciona</p>
    <h2 class="display-xl text-5xl md:text-7xl" data-reveal>Tan fácil como <span class="text-gradient">1, 2, 3</span></h2>
  </div>

  <div class="grid gap-px overflow-hidden rounded-3xl border border-cream/10 bg-cream/10 md:grid-cols-3" data-reveal-group>
    {steps.map((s) => (
      <div data-reveal-item class="bg-ink p-10">
        <span class="display-xl text-7xl text-cream/10">{s.n}</span>
        <h3 class="mt-4 font-display text-2xl font-bold text-cream">{s.title}</h3>
        <p class="mt-2 text-cream/55">{s.text}</p>
      </div>
    ))}
  </div>
</section>
